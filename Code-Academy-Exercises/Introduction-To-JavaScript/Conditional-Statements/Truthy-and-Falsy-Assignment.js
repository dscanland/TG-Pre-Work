let tool = 'marker';

// Use short circuit evaluation to assign  writingUtensil variable below:
let writingUtensil = tool || 'pen';

console.log(`The ${writingUtensil} is mightier than the sword.`);

// I learned truthy and falsy assignments or short circuit evaluation to create a short hand way from evaluating non-boolean if...else statements
