let newVariable = 'Playing around with typeof.';

console.log(typeof newVariable);

newVariable = 1;
console.log(typeof newVariable);

// I learned that you can use the typeof operator to identify what data type is being used in the variable
