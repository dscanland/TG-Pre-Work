function makeShoppingList(item1 = 'milk', item2 = 'bread', item3 = 'eggs'){
  console.log(`Remember to buy ${item1}`);
  console.log(`Remember to buy ${item2}`);
  console.log(`Remember to buy ${item3}`);
}

// I learned that you can enter default parameters in a function so that if you do not have a argument or have an undefined then the default paramenter will create the predetermined value
