function logVisibleLightWaves() {
  const lightWaves = 'Moonlight'
  console.log(lightWaves);
}
logVisibleLightWaves()
console.log(lightWaves);

// I learned that block scope is a variable defined inside a block and is only accessible to the lines of code within the block
